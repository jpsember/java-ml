enum DataType;

fields {

  // Name of tensor (e.g. variable name)
  //
  string name;

  DataType data_type;

  *int shape;

  // If > 0, image of a labelled image pair
  int image_index;

  // If > 0, index of a labelled image pair
  //
  int label_index;
}
